# =============================================================================
# Product Service Configuration
# =============================================================================

# =============================================================================
# Service Configuration
# =============================================================================
ENVIRONMENT=development
DEBUG=true
NAME=product-service
VERSION=1.0.0
PORT=8001

# =============================================================================
# Logging Configuration
# =============================================================================
LOG_LEVEL=INFO
LOG_FORMAT=console
LOG_TO_CONSOLE=true
LOG_TO_FILE=true
LOG_FILE_PATH=./logs/product-service.log

# =============================================================================
# Database Configuration
# =============================================================================
# MongoDB connection string (used in Azure Container Apps via Key Vault)
# For local development, use Dapr secret store (.dapr/secrets.json) instead
# MONGODB_URI=mongodb://admin:admin123@localhost:27019/productdb?authSource=admin
# MONGODB_DATABASE=productdb

# =============================================================================
# Dapr Configuration
# =============================================================================
DAPR_HOST=localhost
DAPR_HTTP_PORT=3501
DAPR_GRPC_PORT=50001
DAPR_APP_ID=product-service
DAPR_PUBSUB_NAME=event-bus

# =============================================================================
# Direct Service URLs (when DAPR_ENABLED=false)
# =============================================================================
DAPR_INVENTORY_SERVICE_APP_ID=inventory-service

# =============================================================================
# Security Configuration
# =============================================================================
# JWT secret is stored in Dapr secret store (.dapr/secrets.json) for local dev
# In Azure, it's injected via Key Vault as JWT_SECRET env var
# JWT_SECRET=your_jwt_secret_key
JWT_ALGORITHM=HS256
JWT_EXPIRATION=3600