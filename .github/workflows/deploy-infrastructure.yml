# Deploy Product Service Infrastructure
# Creates: MongoDB database in Cosmos DB + Key Vault secrets
# Triggers: Manual dispatch or changes to .azure/infrastructure/**

name: Deploy Infrastructure

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - prod
      skipRoleAssignments:
        description: 'Skip role assignments (use deployment script for idempotent creation)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

  push:
    branches:
      - main
    paths:
      - '.azure/infrastructure/**'

permissions:
  id-token: write
  contents: read

env:
  AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
  AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
  AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}

jobs:
  deploy-infrastructure:
    name: Deploy Infrastructure
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment || 'dev' }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ env.AZURE_CLIENT_ID }}
          tenant-id: ${{ env.AZURE_TENANT_ID }}
          subscription-id: ${{ env.AZURE_SUBSCRIPTION_ID }}

      - name: Set environment variables
        id: env
        run: |
          ENV="${{ github.event.inputs.environment || 'dev' }}"
          SKIP_ROLES="${{ github.event.inputs.skipRoleAssignments || 'false' }}"
          echo "environment=$ENV" >> $GITHUB_OUTPUT
          echo "resource_group=rg-xshopai-$ENV" >> $GITHUB_OUTPUT
          echo "skip_role_assignments=$SKIP_ROLES" >> $GITHUB_OUTPUT

      - name: Deploy Infrastructure
        uses: azure/arm-deploy@v2
        with:
          subscriptionId: ${{ env.AZURE_SUBSCRIPTION_ID }}
          resourceGroupName: ${{ steps.env.outputs.resource_group }}
          template: .azure/infrastructure/main.bicep
          parameters: >
            environment=${{ steps.env.outputs.environment }}
            skipRoleAssignments=${{ steps.env.outputs.skip_role_assignments }}
          failOnStdErr: false
          deploymentName: product-service-infra-${{ github.run_number }}

      - name: Deployment Summary
        run: |
          echo "## Infrastructure Deployment Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Resource | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| MongoDB Database (productdb) | ✅ Deployed |" >> $GITHUB_STEP_SUMMARY
          echo "| Products Collection | ✅ Created |" >> $GITHUB_STEP_SUMMARY
          echo "| Key Vault Secrets (mongodb-uri, mongodb-database) | ✅ Configured |" >> $GITHUB_STEP_SUMMARY
          echo "| Role Assignments | ${{ steps.env.outputs.skip_role_assignments == 'true' && '⏭️ Skipped' || '✅ Created (idempotent)' }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "> **Note:** Role assignments now use Azure CLI deployment script for idempotency - safe to re-run" >> $GITHUB_STEP_SUMMARY
